#include<stdio.h>
#include<stdbool.h> //Must include this header file for using bool in c

int main()
{
    bool b = false;  

    printf("%d",b);  //if b is false it will return 0, if true will get 1. 
                    //It doesnt except personal format splecifier.
    return 0;
}